# 文档

| 文件 | 内容 | 版本 |
|---|---|---|
| [`tech-design.html`](./tech-design.html) | **技术设计说明书** —— 11 章 + 2 附录 | V1.1 · 2026-08-11 |
| [`design-rbac.md`](./design-rbac.md) · [`.html`](./design-rbac.html) | **角色与权限设计** —— 27 个权限点、5 角色、四阶段落地路径 | V1.4 · 2026-09-06 |
| [`design-resume.html`](./design-resume.html) | 任务中断恢复设计 | V1.1 · 2026-08-14 |
| [`design-replay-api.html`](./design-replay-api.html) | 判定链路回放接口设计 | V1.1 · 2026-08-14 |
| [`design-quota-multi-replica.html`](./design-quota-multi-replica.html) | 每日配额多副本设计 | V1.1 · 2026-08-18 |
| [`prototype.html`](./prototype.html) | **产品原型** —— 四个产品阶段的完整控制台 | 2026-09-06 |
| [`test-cases.md`](./test-cases.md) · [`test-report.md`](./test-report.md) | 用例清单与测试报告 | 2026-08-18 |

HTML 都是单文件、无外部依赖，**下载后用浏览器直接打开**即可。
GitHub 网页上只会显示源码，不会渲染。

```bash
# 克隆后本地打开
open docs/tech-design.html
open docs/prototype.html
```

---

## ⚠️ 文档中的数字均为占位值

设计文档与原型中出现的**全部指标均为设计阶段的占位值，不是实测结果**：

- 执行准确率 `89.2%`、静态校验拦截率 `34.6%`、单次成本 `¥0.011`
- 消融实验四组数据、各步骤耗时与 token 数
- 原型中的查询结果表格（基于样例库结构虚构）

这些数字用于说明**将要度量什么**，而非**已经度量到什么**。
**这些文档不会被回填** —— 它们是当时的设计稿，改掉就失去了对照价值。

实测结果在主 README 的「实测结果」一节，P3 完成后已按承诺公开：盲测集成绩
（实现全程不可见、一次性运行，该成绩即最终成绩）、未经筛选的失败样本分类分布、
以及各消融组与准确率同时报告的成本与延迟代价。原始结果在 `evals/results/`。

主 README 的原则同样适用：**不会出现未经实测的指标。**

---

## 技术设计说明书 · 章节导引

| 章 | 内容 |
|---|---|
| 1 | 概述 —— 定位、目标与非目标 |
| 2 | 总体架构 —— 9 步链路、模块划分、技术选型及被排除项 |
| 3 | **数据接入** —— 连接层 / Schema 层 / 语义层三层设计 |
| 4 | **护栏设计** —— 17 条规则、强制改写、租户隔离双层方案（现已 18 条，V1.1 之后补入 R-19 数据期限窗口，见主 README） |
| 5 | 状态机 —— LangGraph 节点与边、多步查询规划 |
| 6 | **评测方案** —— 黄金集、指标定义、消融实验、防自证机制 |
| 7 | 可观测与成本归因 |
| 8 | **生产使用边界** —— 三种场景结论 + 8 项准入条件 |
| 9 | 分阶段计划 P0–P5 |
| 10 | 风险与未决事项 |
| 11 | Agent 能力使用说明 —— 含刻意未使用的能力及理由 |
| 附录 A/B | 配置文件样例 / 数据库初始化脚本 |

第 4、6、8 章是核心：**护栏怎么做、怎么证明它有效、什么情况下不该用。**

---

## 产品原型

原型是**产品目标**，不是当前版本的说明 —— 它有意画得比已建成的多。三组导航：

| 分组 | 页面 | 状态 |
|---|---|---|
| Workspace · 阶段一 | 查询工作台 / 任务中心 / 数据源 | 已建成 |
| Governance · 阶段二 | 身份与权限 / 业务口径 / Agent 质量中心 / 执行追踪 / 审计中心 | 已建成（「高成本审批」入口 2026-09-06 撤下，页面与接口保留） |
| Scale · 阶段三/四 | Connector 节点 / 开发者工具 / 产品落地路线 | **未实现，无后端** |

看原型时优先看这几处，它们是这个项目区别于"能写 SQL 的聊天框"的地方：

1. **查询工作台的右栏** —— 生成的 SQL 里，租户谓词与 `LIMIT` 被标为系统强制注入，
   一眼可辨哪部分是模型写的、哪部分是护栏加的。
2. **任务中心的断点续跑** —— 从检查点恢复，checkpoint 之前的节点不重跑。
3. **业务口径中心** —— 模型永远推不出来的那一层，也是消融实验里唯一有方向性收益的一组。
4. **审计中心与执行追踪** —— 每次调用一条记录，包括被护栏拦下的那些。

与已上线控制台的对应关系见主 README 的「Web 控制台」一节。
